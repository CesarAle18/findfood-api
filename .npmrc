# next-forge is developed against bun/pnpm; npm's strict peer resolver rejects
# a couple of upstream ranges (openai 4.x wants zod ^3 as an optional peer,
# while the monorepo is on zod 4). Ignore peer ranges and pin the versions that
# actually matter via "overrides" in package.json (react, react-dom).
legacy-peer-deps=true
